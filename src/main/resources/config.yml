# ==========================================
# AnimalHide 核心配置文件
# 文本支持 MiniMessage 语法
# ==========================================

# 插件消息前缀
prefix: "<gray>[<aqua>动物躲猫猫</aqua>]</gray> "

# 对局内玩家碰撞由插件强制开启（计分板队伍 ah_collide + setCollidable）。
# 若使用 TAB，请勿再对本小游戏世界关闭碰撞（勿依赖 scoreboard-teams.enable-collision=false 作用于对局世界）。

# 主大厅
main-lobby:
  world: "world"
  x: 0.5
  y: 64.0
  z: 0.5
  yaw: 0.0
  pitch: 0.0
